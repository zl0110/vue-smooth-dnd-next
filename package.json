{
  "name": "monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn workspace example dev",
    "deploy": "yarn workspace example deploy",
    "lib:watch": "yarn workspace vue-smooth-dnd-next run build:watch",
    "lib": "yarn workspace vue-smooth-dnd-next run build"
  }
}
